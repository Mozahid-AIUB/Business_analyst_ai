# Generated by `node build.js` — reproducible, so not tracked
dist/

# Dependencies (the app itself has none; this is for tooling only)
node_modules/
package-lock.json

# OS and editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log

# Scratch left by extraction work
assets/*.tmp

# Original, full-size source photos - only the resized copies in
# assets/team/ are actually served by the site
image/
